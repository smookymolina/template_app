GOAL: Update production with a safe git pull - BACKUP FIRST, then update code, and restart services.

=============================================================================
CHANGES IN THIS VERSION (v1.5.7.1 - 2026-02-09):
=============================================================================
*** PROBADO Y FUNCIONANDO EN DESARROLLO ***

1) MEJORAS CRÍTICAS EN CALENDARIO Y NOTIFICACIONES:
   - Fix CRITICO: Errores de visualización de fechas en el calendario debido a inconsistencias de zonas horarias entre frontend y backend. Las fechas de entrevistas ahora se muestran correctamente.
   - Fix CRITICO: Indicador de notificaciones no funcional/confuso. La campana de notificaciones ahora muestra un punto rojo simple como indicador visual de nuevas notificaciones.
   - Backend: Columna `fecha` del modelo `Entrevista` actualizada de `db.Date` a `db.DateTime(timezone=True)`. Eliminada la columna `hora` separada, consolidando fecha y hora en un solo campo timezone-aware.
   - Backend: API de `entrevistas` actualizada para manejar y almacenar correctamente los objetos `DateTime` con zona horaria.
   - Frontend: Refactorización de `static/js/calendar.js` para mejorar la legibilidad y mantenimiento, extrayendo la generación de HTML compleja en funciones auxiliares.
   - Frontend: Mejoras de UI/UX para el calendario incluyendo estilos responsivos (`static/css/styles.css`) y atributos de accesibilidad (`aria-label`, `role`) para una mejor experiencia.

   FILES MODIFIED:
   * models/entrevista.py - Cambio de tipo de columna 'fecha', eliminacion de 'hora', timezone-aware DateTime.
   * routes/api.py - Manejo de DateTime con timezone en endpoints de entrevistas.
   * static/js/calendar.js - Corrección de lógica de fechas, refactorización de funciones.
   * static/js/notification-bell.js - Lógica simplificada para indicador de notificaciones.
   * static/css/styles.css - Estilos responsivos para calendario.
   * static/css/notifications.css - Estilos para el nuevo indicador de notificaciones.

=============================================================================
CHANGES IN THIS VERSION (1.5.7.0 - 2026-02-09):
=============================================================================
*** PROBADO Y FUNCIONANDO EN DESARROLLO ***

1) AUDITORIA COMPLETA DE LINEA DE SEGUIMIENTO:
   - Fix CRITICO: Permisos de gerentes en get_by_id() - podian acceder reclutas de otros equipos
   - Fix CRITICO: XSS en timeline (item.title, info.nombre sin escapeHtml)
   - Fix: Info leakage - recluta_id expuesto en API publica de timeline
   - Fix: Timezone bugs en client.js (new Date("YYYY-MM-DD") parseaba como UTC)
   - Fix: Normalizacion inconsistente de folios en endpoints publicos
   - Fix: Dead code en serialize() asesor_nombre fallback inalcanzable
   - Eliminacion de codigo muerto legacy (loadTimelineData, saveTimelineItem, etc.)

2) PERFIL DE RECLUTA EN TRACKING PUBLICO:
   - Nuevo diseno de tarjeta de perfil con foto, nombre, puesto, estado
   - foto_url y puesto incluidos en API /api/tracking/<folio>
   - Funcion buildFotoUrl() para construccion correcta de URL de fotos
   - Modal expandible automaticamente al mostrar resultados
   - CSS completo con responsive (600px), dark mode, animaciones
   - Filtros inline en timeline del cliente (Todos/Completados/Pendientes)

3) HOTFIX UI + FOTO + TRACKING (ESTABILIDAD):
   - Seguimiento publico: comentarios se despliegan solo al hacer click en el evento
   - Seguimiento publico: fix visual (timeline sin linea encimada sobre cards)
   - Reclutas: botones de acciones (ver/editar/eliminar) sin iconos duplicados
   - Reclutas: click en folio copia el folio completo
   - Configuracion: recorte de foto antes de subir (perfil usuario)
   - Reclutas: guardado con validacion robusta (evita falsos "Error de validacion")

   FILES MODIFIED:
   * models/recluta.py - Fix permisos gerente en get_by_id(), fix serialize()
   * routes/api.py - Normalizacion folios, foto_url/puesto en tracking, quitar recluta_id publico
   * routes/main.py - Normalizacion folios, redirect con folio normalizado
   * static/js/reclutas.js - XSS fix en renderTimelineItem, eliminar dead code
   * static/js/client.js - Rediseno tracking, XSS fixes, timezone fixes, comentarios toggle
   * static/js/photo-cropper.js - Recorte robusto, soporte perfil usuario
   * static/js/configuracion.js - Recorte antes de subir foto
   * static/css/new-timeline.css - Perfil tracking, modal expandible, fix timeline publico
   * static/css/styles.css - Estilos foto perfil, botones acciones, folio copiable
   * templates/index.html - Modal clase modal-tracking

=============================================================================
PREVIOUS VERSIONS:
=============================================================================
v1.5.6.9 (2026-02-09):
   - Photo Lightbox - Ver fotos ampliadas con click
   - Placeholder SVG en lugar de /api/placeholder

v1.5.6.8 (2026-02-03):
   - Fix: Cambio de estado de reclutas para gerentes
   - Tracking de tiempo de uso activo (Activity Tracker)
   - Nuevas columnas: active_time_seconds, last_heartbeat en user_session

=============================================================================

###########################################################################
#                                                                         #
#   INSTRUCCIONES DE ACTUALIZACION EN PRODUCCION (7 PASOS)               #
#   Linux / Nginx + Gunicorn / MySQL o SQLite                            #
#                                                                         #
###########################################################################

ANTES DE EMPEZAR - Confirmar:
   - Ruta del repositorio: /var/www/template_app (o la actual)
   - Branch destino: 1.5.7.0
   - Servicio Gunicorn: gunicorn o template_app
   - Base de datos: SQLite (instance/template_app.db) o MySQL
   - Token de GitHub (necesario para autenticacion HTTPS)

=============================================================================
PASO 1: BACKUP COMPLETO DE DATOS (CRITICO - HACER PRIMERO)
=============================================================================
   *** SIN BACKUP, NO CONTINUAR. ESTO ES OBLIGATORIO. ***

   cd /var/www/template_app
   TS=$(date +%Y%m%d_%H%M%S)
   BACKUP_DIR=/var/backups/template_app/$TS
   mkdir -p "$BACKUP_DIR"

   # 1A) Backup de base de datos (PRIORIDAD MAXIMA)
   # -----------------------------------------------
   # SQLite:
   cp instance/template_app.db "$BACKUP_DIR/template_app.db"

   # MySQL (alternativa):
   # set -a && [ -f .env ] && . ./.env && set +a
   # mysqldump -u "${MYSQL_USER}" -p"${MYSQL_PASSWORD}" -h "${MYSQL_HOST}" "${MYSQL_DB}" > "$BACKUP_DIR/db.sql"

   # 1B) Backup de archivos subidos (fotos, documentos)
   # ---------------------------------------------------
   tar -czf "$BACKUP_DIR/uploads.tar.gz" uploads/

   # 1C) Backup de estado de migraciones
   # ------------------------------------
   source .venv/bin/activate
   flask db current > "$BACKUP_DIR/db_current.txt" 2>&1 || echo "No migrations" > "$BACKUP_DIR/db_current.txt"

   # 1D) Export de datos a CSV (opcional pero recomendado)
   # ------------------------------------------------------
   python scripts/export_data.py 2>/dev/null && \
   LATEST=$(ls -dt backup_* 2>/dev/null | head -1) && \
   [ -n "$LATEST" ] && mv "$LATEST" "$BACKUP_DIR/"

   # Verificar que el backup existe
   ls -la "$BACKUP_DIR"
   echo "Backup completo en: $BACKUP_DIR"

=============================================================================
PASO 1B: RESUMEN RAPIDO POST-BACKUP (COPIAR/PEGAR)
=============================================================================
   # SOLO ejecutar despues del PASO 1 completado
   cd /var/www/template_app
   git status -sb
   git stash push -u -m "prod-pre-update-$TS"
   git fetch --all --prune
   git checkout 1.5.7.0 2>/dev/null || git checkout -b 1.5.7.0 origin/1.5.7.0
   git pull --ff-only origin 1.5.7.0
   source .venv/bin/activate
   python -m pip install -r requirements.txt --no-cache-dir
   flask db upgrade 2>&1 | tee "$BACKUP_DIR/migration.log"
   # Verificar columnas v1.5.6.8 si aplica (ver PASO 4C)
   sudo systemctl restart gunicorn
   sudo systemctl restart nginx
   # Actualizar permisos (PASO 7)
   sudo chown -R www-data:www-data /var/www/template_app/instance/
   sudo chown -R www-data:www-data /var/www/template_app/uploads/
   sudo chmod 755 /var/www/template_app/instance/
   sudo chmod 664 /var/www/template_app/instance/*.db 2>/dev/null || true

=============================================================================
PASO 2: VERIFICAR ESTADO DEL REPOSITORIO
=============================================================================
   cd /var/www/template_app

   # Verificar que estamos en el directorio correcto
   git rev-parse --show-toplevel

   # Ver estado actual
   git status -sb
   git branch --show-current

   # Si hay cambios locales, hacer stash:
   git stash push -u -m "prod-pre-update-$TS"

   # Verificar conexion al remoto
   git remote -v

=============================================================================
PASO 3: CONFIGURAR AUTENTICACION Y ACTUALIZAR CODIGO (GIT PULL)
=============================================================================
   cd /var/www/template_app

   # 3A) Configurar autenticacion con token de GitHub
   # -------------------------------------------------
   # Si git pull pide autenticacion, configurar el remote con token:
   #
   #   git remote set-url origin https://<TU_TOKEN>@github.com/smookymolina/template_app.git
   #
   # Para generar un token: GitHub > Settings > Developer settings >
   #   Personal access tokens > Tokens (classic) > Generate new token
   #   Permisos necesarios: repo (Full control of private repositories)
   #
   # Verificar que el remote esta configurado:
   git remote -v

   # 3B) Obtener cambios del remoto
   git fetch --all --prune

   # 3C) Cambiar al branch destino si es necesario
   git checkout 1.5.7.0 2>/dev/null || git checkout -b 1.5.7.0 origin/1.5.7.0

   # 3D) Actualizar (solo fast-forward, sin merge manual)
   git pull --ff-only origin 1.5.7.0

   # Si falla el ff-only, DETENER y revisar manualmente
   # NUNCA hacer merge en produccion sin revision

=============================================================================
PASO 4: ACTUALIZAR DEPENDENCIAS Y ESQUEMA DE BD
=============================================================================
   cd /var/www/template_app
   source .venv/bin/activate

   # 4A) Actualizar dependencias Python
   python -m pip install --upgrade pip
   python -m pip install -r requirements.txt --no-cache-dir

   # 4B) Ejecutar migraciones de Flask-Migrate
   flask db upgrade 2>&1 | tee "$BACKUP_DIR/migration.log"

   # 4C) Actualizaciones de esquema de BD para Entrevistas (v1.5.7.1)
   # -----------------------------------------------------------------
   # IMPORTANTE: La columna `fecha` en la tabla `entrevista` ha cambiado de `DATE`
   # a `DATETIME WITH TIMEZONE`. Esto permite un manejo preciso de las zonas horarias.
   # La columna `hora` separada ha sido eliminada.
   # El comando `flask db upgrade` ejecutado en el paso 4B ya maneja este cambio
   # automáticamente a través del script de migración generado.
   # No se requiere acción manual adicional para este cambio de columna.

   # 4D) Verificar esquema de base de datos
   python scripts/diagnostico_db.py 2>/dev/null || echo "Script de diagnostico no disponible"

=============================================================================
PASO 5: VERIFICAR SERVICIOS
=============================================================================
   # 5A) Verificar Redis
   systemctl status redis --no-pager || systemctl status redis-server --no-pager
   redis-cli ping  # Debe responder PONG

   # Si Redis no esta corriendo:
   # sudo systemctl start redis

   # 5B) Verificar MySQL (si aplica)
   # systemctl status mysql --no-pager

   # 5C) Limpiar cache de Redis (opcional)
   redis-cli KEYS "metrics_cache_*" | xargs -r redis-cli DEL

=============================================================================
PASO 6: REINICIAR SERVICIOS Y VALIDAR
=============================================================================
   # 6A) Reiniciar servicios en orden
   sudo systemctl restart gunicorn
   sudo systemctl restart nginx

   # 6B) Verificar estado
   sudo systemctl status gunicorn --no-pager
   sudo systemctl status nginx --no-pager

   # 6C) Validacion rapida
   curl -I https://tu-dominio

   # 6D) Validaciones especificas de v1.5.7.0:
   #
   # TRACKING PUBLICO CON PERFIL:
   #   - Abrir pagina principal sin login
   #   - Click en "Seguimiento" o boton de consulta
   #   - Ingresar un folio valido (REC-XXXXXXXX)
   #   - Debe mostrar: foto del candidato, nombre, puesto, estado, fechas
   #   - Debe mostrar: linea de tiempo con filtros (Todos/Completados/Pendientes)
   #   - Los filtros deben funcionar correctamente
   #   - "Nueva consulta" debe resetear todo
   #
   # PERMISOS DE GERENTE:
   #   - Login como gerente
   #   - Solo debe ver reclutas de su equipo (no todos)
   #   - Intentar acceder a recluta de otro equipo -> debe fallar
   #
   # SEGURIDAD XSS:
   #   - Crear un evento con titulo: <script>alert(1)</script>
   #   - Debe mostrarse como texto, NO ejecutarse
   #
   # PHOTO LIGHTBOX (v1.5.6.9):
   #   - Click en foto de perfil -> debe abrirse en grande
   #   - Click fuera o Escape -> debe cerrarse
   #
   # ACTIVITY TRACKER (v1.5.6.8):
   #   - DevTools (F12) -> Console
   #   - Debe aparecer: "[ActivityTracker] Activity Tracker modulo cargado"

   # 6E) Si usaste stash, revisar:
   git stash list
   # Decidir si aplicar: git stash pop

=============================================================================
PASO 7: ACTUALIZAR PERMISOS (ULTIMO PASO - OBLIGATORIO)
=============================================================================
   # Este paso SIEMPRE debe ejecutarse al final para evitar errores 500.

   # 7A) Permisos de base de datos
   sudo chown -R www-data:www-data /var/www/template_app/instance/
   sudo chmod 755 /var/www/template_app/instance/
   sudo chmod 664 /var/www/template_app/instance/*.db 2>/dev/null || true

   # 7B) Permisos de archivos subidos (fotos, documentos)
   sudo chown -R www-data:www-data /var/www/template_app/uploads/
   sudo chmod -R 755 /var/www/template_app/uploads/

   # 7C) Permisos de archivos estaticos (por si git cambio ownership)
   sudo chown -R www-data:www-data /var/www/template_app/static/
   sudo chown -R www-data:www-data /var/www/template_app/templates/

   # 7D) Verificar que todo funciona despues de permisos
   curl -I https://tu-dominio
   echo "Permisos actualizados. Despliegue completo."

=============================================================================

TROUBLESHOOTING:
=============================================================================
A) Error de autenticacion en git pull/push:
   - CAUSA: Remote URL sin token o token expirado
   - SOLUCION:
     git remote set-url origin https://<TU_TOKEN>@github.com/smookymolina/template_app.git
   - VERIFICAR: git remote -v (debe mostrar URL con token)
   - GENERAR TOKEN: GitHub > Settings > Developer settings > Personal access tokens

B) Error 500 despues de git pull:
   - CAUSA: Permisos de archivos
   - SOLUCION:
     sudo chown -R www-data:www-data /var/www/template_app/instance/
     sudo chown -R www-data:www-data /var/www/template_app/uploads/
     sudo systemctl restart gunicorn

C) Error "no such column: user_session.active_time_seconds":
   - CAUSA: Columnas no agregadas
   - SOLUCION: Ejecutar paso 4C

D) Fotos de perfil no se ven en tracking publico:
   - Verificar que foto_url existe en la BD del recluta
   - Verificar permisos de uploads/ (paso 7B)
   - Fotos se sirven en /media/profiles/<filename>
   - Limpiar cache del navegador (Ctrl+Shift+R)

E) Lightbox no funciona:
   - Limpiar cache del navegador
   - Verificar que photo-cropper.js carga (Network tab -> 200 OK)
   - Verificar consola por errores JavaScript

F) Activity Tracker no registra tiempo:
   - Verificar columnas en user_session (paso 4C)
   - Verificar que activity-tracker.js carga correctamente

G) CSS/estilos antiguos:
   - Limpiar cache Nginx: sudo rm -rf /var/cache/nginx/*
   - Reiniciar Nginx
   - Hard refresh en navegador (Ctrl+Shift+R)

H) ROLLBACK (si algo falla):
   - Restaurar base de datos:
     cp "$BACKUP_DIR/template_app.db" instance/template_app.db
   - Restaurar uploads:
     tar -xzf "$BACKUP_DIR/uploads.tar.gz" -C /
   - Regresar a version anterior:
     git checkout 1.5.6.9
   - Reiniciar servicios:
     sudo systemctl restart gunicorn && sudo systemctl restart nginx
   - Actualizar permisos:
     sudo chown -R www-data:www-data /var/www/template_app/instance/
     sudo chown -R www-data:www-data /var/www/template_app/uploads/

=============================================================================

NOTAS IMPORTANTES:
- SIEMPRE hacer backup ANTES de cualquier cambio (PASO 1)
- SIEMPRE actualizar permisos AL FINAL de todo (PASO 7)
- NUNCA eliminar la base de datos directamente
- El backup permite rollback inmediato si algo falla
- Redis debe estar corriendo para metricas y cache
- Los archivos estaticos pueden estar en cache del navegador
- Sesiones antiguas (antes de v1.5.6.8) usaran calculo legacy de tiempo
